# Minor updates

> **Status 2026-09-20.** Live. Newest last. The history audits live here, and
> each one is dated rather than standing: the newest is the only one that
> describes the repository as it is.

One entry per change too small for `DECISIONS.md` and not worth editing
`CLAUDE.md` for. Newest last.

The original reason for this file was that editing `CLAUDE.md` invalidated the
manifests, so churn came here instead. That reason went on 2026-09-12, three
entries down: the anchored manifests date the design and git carries everything
after, so a document edit implies no restamp. The file kept going because the
other half of its job turned out to be the durable one. A history audit is a
dated statement rather than a standing one, and this is where the dated ones
sit in order.

"One line" is also no longer true and is not being enforced. The audits run long
because what they rule out is the content.

- 2026-09-12 Manifests and proofs moved from the repo root to `_attest/`. Verify from the repo root, e.g. `shasum -a 256 -c _attest/MANIFEST_FINAL_2026-09-12_0020.sha256`, since `shasum -c` resolves listed paths against the working directory and not the manifest's location.
- 2026-09-12 Seed documents are direction, not specification. `BRIEF.md`, `DECISIONS.md` and `VALIDATION.md` inform intent; they are not a spec to conform to and their inconsistencies are not defects to reconcile before building. The tests in `tests/test_invariants.py` remain hard constraints, which is the whole reason the premise survives the docs being loosened.
- 2026-09-12 Stamping is not repeated for document changes. The anchored manifests date the design; git history carries everything after, and editing `CLAUDE.md` no longer implies a restamp.
- 2026-09-13 The Jinja frontend is retired. `web/templates/` and the HTML-emitting half of `src/controlbun/render.py` are deleted; the shared view logic moved to `src/controlbun/views.py` and the ordering display names to `src/controlbun/order.py`. Astro is the frontend.
- 2026-09-13 The repo section of `CLAUDE.md` is scoped to a private backup and needs amending when the repo goes public, because GitHub Pages publishes from a branch or an Actions workflow and that section currently rules out both. Recorded here rather than edited in, so the manifests stay valid until the amendment is actually made.
- 2026-09-13 Name and namespaces registered: `controlbun.com` and `controlbun.org` at Porkbun, `github.com/controlbun`, `huggingface.co/controlbun`. All on the personal account and the personal email, none on the Northeastern address. PyPI and npm are deliberately not held yet: neither allows reserving a name without publishing a package, and the squatting risk is near zero until the name is public. Hold them before the name appears anywhere public, and run a USPTO knockout search before that too.
- 2026-09-13 Trademark filing deliberately deferred, not skipped. `controlbun` returns zero on a USPTO wordmark search; the general search's 2,567 hits are fuzzy matches on `control`, which is crowded in the software classes, and a crowded term is mildly in our favor since nobody owns it. The filing basis will be intent-to-use, Section 1(b): it can be filed before any use in commerce and its filing date becomes a constructive date of first use, which is what closes the gap left by having no use yet. Classes 009 and 042 only, because an ITU requires bona fide intent for everything listed and padding classes is a vulnerability rather than free optionality. Cost is $350 per class at filing and $150 per class later for the Statement of Use, so $700 now and $300 later; pick goods descriptions through the ID Manual selector rather than typing or pasting into the free-form box, which adds $200 per class. Fees are non-refundable, so the wait buys certainty about the name for the price of a month of priority. **Trigger, not a date: file before `controlbun` appears anywhere public with real traffic**, and do npm and PyPI package names in the same sitting since all three are gated on the same event.
- 2026-09-13 Anchor-file drift audit. `BRIEF.md` line 446 still reads "Postgres for metadata" and line 379 still describes the Comparison matrix; both are superseded in `DECISIONS.md` and neither is being edited, because seed documents are direction rather than specification and their inconsistencies are not defects to reconcile. Noted here so the next reader knows the supersession exists without having to find it. `CONTEXT.md` and `BRIEF.md` references to `_falsifier/verify.py` point at the arena as precedent and are correct, not stale.
- 2026-09-13 `controlbun/fetch-test` on the Hub holds a 96-byte tensor whose only job is to prove the pinned-fetch path works against the real host. No metadata block, a tensor called `t`, the numbers 0 through 7. Commit `c1e0da3fb595874da299e783c7ad9c08a95ce7e9`. `.safetensors` is LFS-tracked by the default gitattributes on a model repo regardless of size, so even that file took the full Git LFS flow and comes back through a CDN redirect, which is what made it worth uploading: every real artifact will do the same.
- 2026-09-13 `MANIFEST_TREE_<date>_<time>` is a new kind of attestation alongside `MANIFEST_FINAL` and `MANIFEST_BRIEF`: every tracked file rather than the design documents alone, so it dates the implementation and not just the intent. It covers the existing `_attest` contents too, which chains the earlier proofs into it rather than leaving them beside it. Built from `git ls-files`, so `shasum -a 256 -c` must be run from the repo root. `MANIFEST_FINAL` keeps its old meaning exactly; reusing that name would have made three notarized records mean something different from the fourth.
- 2026-09-15 History audit re-run at `d3ed27a`, 82 commits across all branches, which is `V1.md` A1 and the dated re-run the 2026-09-13 Pages entry asks for. Clean. `_local/`, `.env`, `.env.*`, `NOTES-private.md` and key-shaped filenames have never been added on any branch at any point. No credential pattern in the content of any commit. No personal, financial or immigration term in any tracked file or commit message except `CLAUDE.md` and `V1.md` stating the rule itself. One author identity, the personal address, consistent with the deliberate separation from the Northeastern one. `git check-ignore` confirms all eight private paths. 133 tracked files would become public. This result is dated, not permanent: re-run at the tip on the day the repo is flipped.
- 2026-09-16 Repo transferred from `soham-padia/registry` to `controlbun/registry`, still private. Done before going public rather than after, because a transfer while private costs nothing and a transfer after costs URL churn and stale links. Nothing was lost: zero stars, forks and issues, and no tracked file referenced the old path, the `soham-padia/...` hits in `CONTEXT.md`, `BRIEF.md`, `artifacts/REAL.md` and `artifacts/source.py` all being `steering-arena`, which is a different repo. GitHub redirects the old path. `git remote set-url` done locally; any other clone needs the same. Pages will serve from the org, and because `controlbun.com` is the custom domain, `astro.config.mjs` keeps `base: '/'` rather than needing `/registry`.
- 2026-09-16 HF OAuth app `controlbun` registered, and the identity question in `V2.md` is answered rather than assumed. HF is a full OIDC provider, discovery at `huggingface.co/.well-known/openid-configuration`, RS256, PKCE via S256, usable from any site rather than only a Space. Confidential app with a secret rather than public, because discovery advertises only `client_secret_basic` and `client_secret_post` and Supabase reads that document; Supabase is the confidential client so no function of ours holds anything. Scopes are `openid`, `profile`, `read-memberships` and nothing else: no repo scopes, because `fetch.py` sends no credentials by design and everything the registry points at is public by construction. Token expiry 8 hours, the default, since submitting is occasional and nothing server-side holds an HF token. Device-code flow run end to end; userinfo returns `sub`, `preferred_username` and `orgs` with `roleInOrg`, so org namespace claiming is a lookup. Credentials in `.env` as `HF_OAUTH_CLIENT_ID` and `HF_OAUTH_CLIENT_SECRET`; nothing in the repo reads them and their destination is the Supabase provider config. Redirect URIs so far: `http://localhost/auth/v1/callback` only, since the Supabase callback needs a project ref that does not exist yet.
- 2026-09-16 Supabase custom OIDC provider `huggingface` configured and a real sign-in completed, so the flow works end to end: `custom:huggingface` redirects to HF with the right client id, scopes and callback, and a user row lands in `auth.users`. One finding that changes `V2.md` section 1: `raw_user_meta_data` carries `preferred_username` and `sub` but **`orgs` is null**, because it is an HF-specific claim rather than a standard OIDC one. Org membership therefore has to be read from a live userinfo call at sign-in, while `provider_token` is available, and stored with a timestamp as dated evidence. Better than caching it at signup, which would have been a mutable fact recorded as permanent.
- 2026-09-16 `CLAUDE.md` repo section amended for going public, discharging obligation 1 of the 2026-09-13 Pages entry. The prohibition on a public push is gone. The prohibition on a hosted build is not, and its reason is unchanged: Pages deploys a locally built `astro/dist` from a branch, so what is served is the artifact that passed `make verify` rather than whatever a runner produced from the same source, and an Actions build would break that equivalence quietly by having the falsifier check one build while readers read another. The history-audit rule is restated as running against the tip rather than the working tree, and as dated rather than permanent.
- 2026-09-16 About page reviewed and left as it stands, with one accuracy fix deferred rather than dropped: it lists "a causal test against norm-matched random directions" among the evidence, and both source documents are narrower. `VALIDATION.md` says "confirmed for +d ... with the -d half withdrawn" and `CONTEXT.md` dates that withdrawal to 2026-08-27 as estimator-dependent. Half of that test was retracted and the page states the whole of it, in a list whose job is to say the direction carries more evidence than most. Separately, "the direction in question" there is the arena's shipped `d_olmo3_L24_logistic` at layer 24, while the three directions on this site are at layer 32 and their causal bake-off was inconclusive, 13, 13 and 9 of 24 generations byte-identical to the unsteered baseline. A reader will connect them. Both are wording changes and both are the author's.
- 2026-09-17 Invariant 7 in `CLAUDE.md` now names artifact file format alongside `kind`, `hook_point`, `profile` and method. `controlbun.ingest` ships two converters and the scan in `tests/test_invariants.py` already refused "unsupported format" as a rejection reason; this puts the rule where the rule lives rather than only where it is enforced. The distinction that matters: two converters is fine, refusing bytes because their format is not on a list is not, and refusing bytes that cannot be verified is a different sentence and is correct. Unlike the 006 digest entry and the write-claim entry, both of which were left out of this list as correctness rather than premise, an open converter set is the plurality premise at field level, which is why it belongs.
- 2026-09-17 Fourth real submission added, `soham/pro-human@L24`, as a trial run of the general ingest path. Layer 24 rather than 32, so it exercises three things the first three could not: `LAYER` stops being a module constant and becomes a property of the artifact; the residual-norm key in the recipe payload is derived from the artifact's own layer rather than hardcoded to 32, since a layer-24 row carrying layer 32's norm would measure a different place in the model; and the similarity matrix refuses an angle between it and its siblings with the reason "different layer or hook point", which is correct and was not arranged. It also carries less evidence than the other three: no confound audit and no transfer ratio, because the arena ran that battery on the layer-32 directions only, so `TRANSFER` and `CONFOUNDS` are read with `.get` and absence renders as absence. Its own `meta` calls it a "Layer-sweep candidate" and Season 2's migration pins it as that season's direction; both are true, and adding the one point of the sweep that shipped does not reopen the settled decision against turning a five-point sweep into five rows. First real `pin` in the corpus alongside the synthetic one: steering-arena froze it for Season 2 scoring. Nothing from `CONTEXT.md`'s "Validation state" paragraph was attached to it, because that paragraph follows "Season 3, live" and is not scoped to a season, so reading it as layer 24's would be a guess.
- 2026-09-19 History audit re-run at the tip, `d63f6a0`, 125 commits across all branches. This is the dated re-run the 2026-09-13 Pages entry asks for and supersedes the 2026-09-16 one at `d3ed27a`. Clean. `_local/`, `.env`, `.env.*`, `NOTES-private.md` and key-shaped filenames have never been added on any branch at any point; `git check-ignore` confirms all four are ignored. No credential pattern in the content of any commit, checked separately for `hf_`, `sk-`, `AKIA`, `ghp_` and PEM headers rather than as one expression, because the combined regex exceeded the matcher's complexity limit and a scan that errors is a scan that found nothing. One author identity, the personal address, consistent with the deliberate separation from the Northeastern one. No personal, financial or immigration term in any tracked file. 158 tracked files would become public, up from 133. **What was new to check this time: the first real third-party-shaped submission is now tracked, so the corpus carries prose about somebody's research infrastructure for the first time.** The cluster path `/work/neu/<allocation-id>/...` is NOT tracked anywhere, because the submission's `artifact_path` was corrected to the repo-relative path before it was written; what survives is a bare `/scratch` inside `license_status`, in a sentence explaining why the model's own LICENSE could not be read, plus a script name and line number in prose. That is generic enough to carry no institutional information and it is on the built page, so it is a deliberate keep rather than an oversight. Re-run this again at the tip on the day the repo is actually flipped.
- 2026-09-19 First read of a real submission's rendered page, `soham/trauma@d61-diffmeans-expository-L34`. One fabrication, fixed in `c023232`: "Confound axes checked" listed four axes on an artifact with no confound data of any kind, because `claimant_view` selected them from `eval_suite WHERE author=?` and every submission by an author inherited every suite that author had written. Five faults found in the same read and **not** fixed, recorded here rather than in `DECISIONS.md` because none is a decision. **The definition renders as one wall of text.** It goes into a single `<p>`, so the author's paragraphs, blank lines and the whole "What this trait is NOT" bullet list collapse into an unbroken run. This is the field the project calls the one that matters most and it is currently the least readable thing on the page. **Two generic captions now misdescribe real data**, which is the same class as the falsifier problems the same day: the coefficient-sweep caption says "This artifact reports its scores at a single coefficient chosen by its author" on a submission reporting no scores at all, and the license caption says "Unresolved means nobody has checked" under a status where the author read the license at a dated URL and recorded one genuinely unresolved sub-clause. Both were written when every row was a fixture that had measured something. **`license_status` lost spaces in transit** and renders "whichgrants", "licensedunder" and "The700-million-monthly-active-users"; a correction means a new version, since submissions are immutable. **L2 norm renders as `1`** where every other figure on the page is four decimals. Worth noting as a pattern rather than five items: every one of these was invisible until a real artifact went through, and four of them are captions or layouts that were correct for a corpus of fixtures and became wrong on contact with somebody's actual work.
- 2026-09-19 History audit re-run at `b8d1579`, 135 commits across all eight refs including both on the remote, 165 tracked files. Supersedes the `d63f6a0` entry above, which it also corrects. **One finding, and it is in the superseded entry itself.** That entry certifies that the cluster path `/work/neu/<allocation-id>/...` is not tracked anywhere, and quotes the path in full in order to say so, so the string is tracked in `minor_updates.md` and sits in two reachable blobs, on `cbd207d` and `4abd755`. It is nowhere else: not in `artifacts/intake.jsonl`, not in `astro/src/data/controlbun.json`, not in the built site. It is not a credential and grants no access; what it discloses is an institutional allocation id beside an affiliation the same file already names three times. Redacting the working tree would not remove it from history, so the choice is to accept it or to rewrite history, and the window for the second closes when the repo flips. **Everything else clean.** `_local/`, `.env`, `.env.*`, `NOTES-private.md` and key-shaped filenames have never been added on any ref at any point, checked against every path in every tree rather than against added files only. No credential pattern in any blob in history, scanned one expression at a time for `hf_`, `sk-`, `AKIA`, `ghp_`, `github_pat_`, `xox`, `glpat-`, `AIza`, `sbp_`, `npm_`, PyPI macaroons, JWT headers and five PEM headers; the PEM scans errored the first time because the pattern starts with a dash and `grep` read it as a flag, which is the failure mode the rule about splitting scans exists to catch, and they were rerun with `-e`. Stronger than a pattern scan and the one that settles it: every value in `.env` was compared literally against every blob in history and none appears. No SSN, card or phone shape in any blob. One author identity, the personal address. `git check-ignore` confirms eleven private paths. **Two gaps found and closed.** `artifacts/memberships.jsonl` was not ignored, though `artifacts/seed.py` says in a comment that it is; the comment was describing an intention. And `astro/dist` carried no `.nojekyll`, so Pages would have run Jekyll over a branch deploy and dropped `_astro/`, serving every page unstyled with no error anywhere; `astro/public/.nojekyll` now reaches the output the same way `CNAME` does, verified by building. **What a reader will be able to learn, stated rather than passed.** The real submission publishes the author's own research program in his words: the trauma-as-event-type premise, Ben-Zion and crowd-enVENT as the two stimulus sets, the estimator, a capture script name and line number, a bare `/scratch`, and roughly a dozen real measured figures. It also publishes, under his name, that every verdict word in that tree is suspended, that the cross-model circumplex claim is withdrawn, and that the licensed word is "reliably non-chance" rather than "valid". That is the project's own standard applied to its author and it reads as such. It publishes a dated, unresolved reading of one Llama 3.3 clause, which is a legal question left open in public rather than answered. The namespace claim publishes the HF subject `62cf4580e7f6014c0ea2450f` and the handle `sohampadia`; the subject is already public, returned as `_id` by `huggingface.co/api/users/sohampadia/overview` without authentication, checked rather than assumed, so binding it discloses nothing the Hub does not. This result is dated, not permanent: re-run at the tip on the day the repo is actually flipped, which is not necessarily today.
- 2026-09-19 History rewritten once, to remove an allocation id, and this is the only rewrite this repository has had. `git filter-repo --replace-text` put `<allocation-id>` in place of the literal across every blob on every ref, rewriting 11 commits. The string reached history through the 2026-09-19 audit entry, which certified the path absent and quoted it in full while doing so, so the certification was what tracked it. **Checked after: the literal appears in no blob on any ref.** Four commit references inside `minor_updates.md` and `GO-LIVE.md` were rewritten from the `commit-map` rather than by hand. A bundle of the pre-rewrite state is at `/tmp/registry-before-rewrite.bundle` and is not durable; the rewrite is. The OTS proofs are unaffected, because they attest file content at a date rather than commit ids, which is the property that made this cheap. Every audit dated before this entry refers to commit ids that no longer exist, and the audit has to be re-run at the new tip before the flip.
- 2026-09-19 Licensed Apache-2.0. The text is the canonical one fetched from apache.org rather than typed, with the appendix boilerplate filled in. `pyproject.toml` carries `license` and `license-files`. Apache over MIT for two reasons that are specific rather than habitual: it carries an explicit patent grant, which matters more for a project whose output is model-derived artifacts than for a library, and `allenai/Olmo-3-1125-32B` is itself Apache-2.0, so four of the five real directions sit under terms compatible with the code that indexes them. `NOTICE` states the thing the license cannot: a steering vector is constrained by the license of the model it was read from, not by the license on this code, and it lists which artifacts are vendored here, which are synthetic, and which one is Llama-derived and therefore lives in its author's own namespace rather than in this repository.
- 2026-09-19 History audit re-run at the flip tip, `65bae59`, 149 commits across all five refs including both on the remote and the `pre-rewrite-2026-09-19` tag, 840 blobs, 249 distinct paths, 181 tracked files. Supersedes the `b8d1579` entry, whose commit ids no longer exist after the rewrite. **Clean, and the one finding the superseded entry carried is gone.** The allocation id literal appears in no blob on any ref, checked exactly, case-insensitively and on the shorter prefix; what remains is the `<allocation-id>` placeholder in the two audit sentences that quoted it. Checked separately against the blobs reachable from `origin/main` alone, which confirms the remote never carried it rather than assuming it. `_local/`, `.env`, `.env.*`, `NOTES-private.md` and twenty-four key-shaped filename patterns have never appeared in any tree on any ref; `git check-ignore` confirms ten private paths, and `node_modules` and `__pycache__` are unignored but untracked. Twenty credential patterns scanned one expression at a time, then five PEM headers with `-F -e` so the leading dash is not read as a flag, then four assignment shapes. Every literal value in `.env`, five of them including the HF OAuth client secret the sign-in work added, compared against every blob: none appears. One author identity, the personal address. No SSN or phone shape; the card-shaped hits are float digits in `controlbun.json`, `ssn` is `className`, `iban` is `InfiniBand`, and the one `h1b` is random bytes inside `brand/mark-dark-1254.png`. **Every scan carries a control that must match, because the complexity limit fired again during this audit** and a bounded-repetition window of the form `.{0,40}` errored on the same matcher that accepted a fixed `.{60}`; the errored scans printed nothing, which is what a clean scan prints, and they were rerun as `git grep` across every commit to get file and line. Thirty-six files become newly public, the largest group being the intake, claim, publish and sign-in work and the `src/controlbun/` rename. `GO-LIVE.md` carries a local absolute path under the macOS username, which is the HF handle already published on the site, kept deliberately because the deploy recipe needs a real path.
- 2026-09-19 History audit re-run at `e6933a9`, 154 commits across 6 refs, superseding the `65bae59` one which the corpus removal voided. Clean. `_local/`, `.env`, `.env.*`, `NOTES-private.md` and key-shaped filenames never added on any ref; six credential patterns scanned one expression at a time; the allocation id absent from every blob on every ref; all five literal `.env` values including the HF OAuth client secret absent from every blob; no personal, financial or immigration term in any tracked file; one author identity, the personal address. 174 tracked files would become public. **Every scan carried a control string that had to match**, which is the practice that came out of three separate occasions today where a matcher errored and printed nothing, and a scan that errors prints exactly what a clean scan prints. Dated, not permanent: re-run at the tip before any further change in visibility.
- 2026-09-19 **The repository is public and the site is live at `controlbun.com`.** Flipped at `6ba10ee` after the audit above came back clean, which is the sequence `V1.md` A1 required: audit at the tip, then push, then flip. 174 files and 155 commits became readable. Pages serves from `gh-pages`, built locally and pushed, and the pre-push hook now refuses that branch unless its tree is byte for byte the `astro/dist` that `make verify` produced, so the served artifact and the gated one cannot come apart. That equivalence was the whole argument for having no hosted build and was an assertion until tonight. DNS at Porkbun: the apex `ALIAS` and the wildcard `CNAME` now point at `controlbun.github.io`; the `MX` pair and the SPF `TXT` were left alone, because deleting them would have taken email forwarding at the domain with them, and an earlier instruction of mine to "delete the parking records" would have done exactly that. `www` 301s to the apex. HTTPS was not yet issued at the time of writing: GitHub requests a certificate once the domain points at it, and until it lands `https://controlbun.com` fails while `http://` serves. The corpus at launch is five real submissions by one author, no synthetic rows, and the site says in its own words that plurality is demonstrated zero times.
